'use strict';

var React = require('react');

var Species = React.createClass({
  render: function() {
    return (
      <div className="filter">
        <h1>Let's do some filtering of InterPro Domains!!</h1>
        <p>This is where the filter UI would go</p>
      </div>
    );
  }
});

module.exports = Species;